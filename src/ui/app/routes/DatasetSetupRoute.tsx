import FrontPageContainer from '../../../components/pages/FrontPageContainer';
import type { DatasetSetupRouteProps } from '../../contracts/routes';

export default function DatasetSetupRoute(props: DatasetSetupRouteProps) {
  return <FrontPageContainer {...props} />;
}
